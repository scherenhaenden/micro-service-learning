import { BankingAccountCompactModel } from './banking-account-compact-model';

describe('BankingAccountCompactModel', () => {
  it('should create an instance', () => {
    expect(new BankingAccountCompactModel()).toBeTruthy();
  });
});
