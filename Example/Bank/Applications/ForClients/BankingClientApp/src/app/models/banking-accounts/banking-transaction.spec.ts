import { BankingTransaction } from './banking-transaction';

describe('BankingTransaction', () => {
  it('should create an instance', () => {
    expect(new BankingTransaction()).toBeTruthy();
  });
});
